# Run the following command to create a virtual environment named .venv
python3 -m venv .venv

# **Activate the virtual environment**:
source .venv/bin/activate

# On Windows:
# .venv\Scripts\activate

# **Verify the virtual environment is activated** by checking the command prompt, which should show `(.venv)` at the beginning.

# You can also use the **Python: Create Environment** command in Visual Studio Code to create a